# =========================
#   CRYPTO SCALPING BOT
#   Binance Futuros (USDM) - TESTNET
#   Now supports plan-driven configuration
# =========================

# --- Modo de operación ---
MODE=paper                 # "paper" o "live" (auto-set by apply_profile.py)

# --- Binance (solo testnet de futuros) ---
BINANCE_TESTNET=true       # true/false (auto-set by apply_profile.py)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Operación del bot ---
STARTING_BALANCE_USDT=2000.0
# 1 = 1% (se convierte internamente a 0.01). Valores <1 se interpretan como fracción directa.
POSITION_SIZE_PERCENT=1.0  # Auto-set by apply_profile.py from plan.yml
DAILY_PROFIT_TARGET_USD=40.0
MAX_DAILY_LOSS_USD=100.0   # Auto-set by apply_profile.py from plan.yml

# --- Leverage/Margin (para LIVE en testnet) ---
LEVERAGE=5                    # Auto-set by apply_profile.py from plan.yml
MARGIN_MODE=ISOLATED          # Auto-set by apply_profile.py from plan.yml

# --- Universo y ciclo ---
TIMEFRAME=1m               # Auto-set by apply_profile.py from plan.yml
MAX_SYMBOLS=15             # Auto-set by apply_profile.py from plan.yml
MIN_24H_VOLUME_USDT=5000000
SLEEP_SECONDS_BETWEEN_CYCLES=5
DAILY_RESET_HOUR_UTC=0

# --- Telegram ---
TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=

# --- Miscelánea ---
LOG_LEVEL=INFO

# =========================
# PLAN-DRIVEN CONFIGURATION
# =========================
# 
# This bot now supports plan-driven configuration via config/plan.yml
# 
# To apply a plan to your .env:
#   python scripts/apply_profile.py --dry-run    # Preview changes
#   python scripts/apply_profile.py --write      # Apply changes
#
# The plan system provides:
# - Dynamic universe selection based on liquidity
# - Risk guardrails for position sizing and daily loss limits  
# - Conservative defaults with graceful fallbacks
# - Separation of operational config from secrets
#
# Key benefits:
# - Safe testnet-ready configuration
# - No need to manually edit .env for operational settings
# - Automatic fallback to paper mode if secrets missing
# - Professional risk management controls
#
